# trip-planner

Experiment notebook: `traveltest.ipynb`

**Safar AI app** (Next.js + FastAPI): see [`app/README.md`](app/README.md)

```bash
cd app && npm install && npm run install:all && npm run dev
```